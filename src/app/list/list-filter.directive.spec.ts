import { ListFilterDirective } from './list-filter.directive';

describe('ListFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFilterDirective();
    expect(directive).toBeTruthy();
  });
});
